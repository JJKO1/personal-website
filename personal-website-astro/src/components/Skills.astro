---
export interface Props {
  t: {
    label: string;
    title: string;
    groups: {
      name: string;
      tags: string[];
    }[];
  };
}

const { t } = Astro.props;
---

<section id="skills">
  <div class="container">
    <p class="section-label">{t.label}</p>
    <h2 class="section-title">{t.title}</h2>
    {t.groups.map((group) => (
      <div class="skill-group">
        <p class="skill-group-name">{group.name}</p>
        <div class="skill-tags">
          {group.tags.map((tag) => <span class="skill-tag">{tag}</span>)}
        </div>
      </div>
    ))}
  </div>
</section>
